#!/bin/bash

git submodule update --init
git submodule update --remote

cp -R .config          ~/
cp -R .vim             ~/
cp    .vimrc           ~/

cp -R .tmux            ~/
cp    .tmux.conf       ~/
cp    .promptline.conf ~/
cp    .tmuxline.conf   ~/

cat   .bashrc >>       ~/.bashrc
